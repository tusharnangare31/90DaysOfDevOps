What is a container and why do we need them?
A container is lightweight, standalone executable software package that include every thing that use to run an application: code, runtime enviroment, libraries, and dependencies. they are isolated from each other and the host system, it is a portable and consitent enviroment to deploy and run applications.  

Containers vs Virtual Machines — what's the real difference?

